# Legacy snapshots

Self-contained copies of older or experimental scraper-related code kept **alongside** the main `website_scraper` package without replacing it.

- **`machine-learning-ms/`** — Original “Machine-Learning-MS” / web scraper training utilities from the Web-Scrapper-ML workspace.

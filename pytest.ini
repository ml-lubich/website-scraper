[pytest]
asyncio_mode = auto
testpaths = tests
filterwarnings =
    ignore:datetime.datetime.utcnow:DeprecationWarning
addopts =
    --cov=website_scraper
    --cov-report=term-missing
    --cov-fail-under=80

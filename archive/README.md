# Archive

Historical snapshots and experiments that are **not** part of the installable `website_scraper` API. They stay in the repo for reference and provenance.

| Path | Contents |
|------|----------|
| `ml_ms_2020/` | Original 2020 “Machine-Learning-MS” / Berkeley Reddit-era scripts and status files. Some files target Python 2 or depend on missing modules (`RedditScraper.py`, etc.). |

For current scraping, use the **`website_scraper.WebScraper`** package and the `website-scraper` CLI (see repository root `README.md`).

# Machine-Learning-MS (2020 snapshot)

Preserved as-is from the original workspace. **Not wired into** `website_scraper.WebScraper`.

- `webscraper.py` — minimal Reddit HTML fetch (older stack).
- `TrainingDataCreator.py` / `Utils.py` — incomplete for Python 3; referenced `RedditScraper.py` and `execfile` (Python 2).

For a maintained Python 3 preview script, see `examples/reddit_berkeley_preview.py` at the repository root.
